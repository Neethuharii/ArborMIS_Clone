<link rel="stylesheet" href="{{ asset('css/search_bar.css') }}">

<div class="search-container">
    <div class="search-box">
        <span class="search-icon">🔍</span>
        <input
            type="text"
            id="{{ inputId }}"
            class="search-input"
            placeholder="{{ placeholder }}"
        >
    </div>
</div>